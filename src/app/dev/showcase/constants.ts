import {Object3D, Vector2} from '@verza/sdk';

export const BOX_RADIUS = 0.05;

export const SHOWCASE_LOCATION = new Object3D();

SHOWCASE_LOCATION.position.set(0, 0.15, 0);

export const SHOWCASE_SIZE = new Vector2(100, 40);

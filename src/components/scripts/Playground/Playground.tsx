import Provider from '@app/components/core/Provider';

const Playground = () => {
  return (
    <Provider>
      <PlaygroundRender />
    </Provider>
  );
};

const PlaygroundRender = () => {
  return <></>;
};

export default Playground;
